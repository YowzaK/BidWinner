export interface items {
    id: string;
    image: string,
    category: string,
    itemName: string,
    itemDescription: string,
    condition: string,
    minimumBid: number,
    currentBids: number,
}